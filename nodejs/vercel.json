{
  "buildCommand": "pnpm run build",
  "devCommand": "pnpm run dev",
  "routes": [
    { "src": "/.*", "dest": "dist/_worker.js" }
  ],
  "builds": [
    { "src": "dist/_worker.js", "use": "@vercel/node" }
  ]
}
